<project name="db4o-test-java1.2" basedir="." default="publish"  xmlns:ivy="antlib:fr.jayasoft.ivy.ant">
	
	<import file="../../build-1.1/common-1.1.xml"/>
	
	<property file="${dir.j.jdk1.2.test}/build.properties" />
	
	<path id="lib.path.id">
        <fileset dir="${ivy.lib.dir}" includes="*.jar"/>
	</path>
	
	<target name="clean">
  	<cleanIvyDir/>
	</target>
	
	<target name="init" depends="clean">
		<tstamp />
		<initIvyDir/>
	</target>
	
	<target name="jar" depends="init, resolve">
		<copyjavatest1.2 destDir="${tempSrcDir}"/>
		<buildjar 
			srcDir="${tempSrcDir}"
			jarPath="${jarPath}" 
			fork="yes"
			classPathRefId="lib.path.id"
			target="1.2"
			source="1.2"
		/>
		<delete dir="${tempSrcDir}" failonerror="true"/>
	</target>
	
	 <target name="configure" depends="init">
        <ivy:configure file="${ivy.conf.dir}/ivyconf.xml" />
   </target>
   
  <target name="resolve" depends="configure">
        <ivy:resolve file="${dir.j.jdk1.2.test}/ivy.xml"/>
   		  <ivy:retrieve pattern="${ivy.retrieve.pattern}" conf="${ivy.configurations}" />
    </target>
    
    <target name="publish" depends="resolve, jar">
        <publishToIvy/>
        <antcall target="clean" />
    </target>
</project>