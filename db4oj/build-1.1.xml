<project name="db4o-java1.1" basedir="." default="publish"  xmlns:ivy="antlib:fr.jayasoft.ivy.ant">
	
	<import file="../build-1.1/common-1.1.xml"/>
	
	<property file="build-1.1.properties" />
	
	<!--
	TODO 
	filehead
	
	<property name="bona" value="tempClassesDir" />
		-->
	
	<target name="echo" >
		<echo message="${defaultTempDir}"/>
	</target>
	
	<target name="clean">
  	<cleanIvyDir/>
	</target>
	
	<target name="init">
		<tstamp />
		<initIvyDir/>
	</target>
	
	<target name="jar" depends="init">
		<property name="file.compiler.jdk1.3.args.optional" value="" />
		
		<delete dir="${defaultTempDir}" failonerror="true"/>
		<mkdir dir="${defaultTempDir}" />
		
		<javac compiler="classic" fork="yes" executable="${file.compiler.jdk1.3}"
			srcdir="${coreSrcDir}" destdir="${defaultTempDir}" debug="off">
			<compilerarg line="${file.compiler.jdk1.3.args} ${file.compiler.jdk1.3.args.optional}"/>
		</javac>
		
		<jar jarfile="${coreJarPath}" basedir="${defaultTempDir}" />
		
		<delete dir="${defaultTempDir}" failonerror="true"/>
	</target>
	
	 <target name="configure" depends="init">
        <ivy:configure file="${ivy.conf.dir}/ivyconf.xml" />
   </target>
   
   <target name="resolve" depends="configure">
        <ivy:retrieve />
    </target>
    
    <target name="publish" depends="resolve, jar">
    	<publishToIvy/>
    </target>
</project>