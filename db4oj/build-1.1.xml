<project name="db4o-java1.1" basedir="." default="jar"  xmlns:ivy="antlib:fr.jayasoft.ivy.ant">
	
	<import file="../build-1.1/common-1.1.xml"/>
	
	<property file="build-1.1.properties" />
	
	<!--
	TODO 
	filehead
	
	<property name="bona" value="tempClassesDir" />
		-->
	
	<target name="echo" >
		<echo message="${defaultTempDir}"/>
	</target>
	
	<target name="clean">
  	<delete dir="${artifactDir}" failonerror="true"/>     
	</target>
	
	<target name="init" depends="clean">
		<tstamp />
		<mkdir dir="${artifactDir}" />
	</target>
	
	<target name="jar" depends="init">
		<buildjar 
			srcDir="${coreSrcDir}"
			jarPath="${coreJarPath}" 
			fork="yes"
			executable="${file.compiler.jdk1.3}"
			target="1.1"
			source="1.1"
			compilerarg = "${file.compiler.jdk1.3.args} ${file.compiler.jdk1.3.args.optional}"
		/>
	</target>
	
	 <target name="configure" depends="init">
        <ivy:configure file="${ivy.conf.dir}/ivyconf.xml" />
   </target>
   
   <target name="resolve" depends="configure">
        <ivy:retrieve />
    </target>
    
    <target name="publish-jar" depends="resolve, jar">
        <property name="revision" value="${db4o.version.dotted}"/>
        <delete file="${artifactDir}/ivy.xml"/>
		<ivy:publish artifactspattern="${artifactDir}/[artifact].[ext]" 
			resolver="projects"
			pubrevision="${revision}" 
			status="release"
		/>
    </target>
</project>