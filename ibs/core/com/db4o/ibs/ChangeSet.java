package com.db4o.ibs;

/**
 * A set of changes that must be applied as whole.
 * 
 * A ChangeSet is comprised of all the changes that occurred
 * during a single transaction.
 */
public interface ChangeSet {

}
