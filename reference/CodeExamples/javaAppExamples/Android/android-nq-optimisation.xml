<?xml version="1.0"?>
<project name="db4o enhance project" default="enhance" basedir=".">
    
    
    <!-- #example: Define the task for the enhancement #-->    
    <path id="project.classpath">
        <pathelement path="${basedir}/bin"/>
    	<!-- The android.plattform points to the Android-platform libraries -->
        <fileset dir="${android.platform}/">
            <include name="android.jar"/>
        </fileset>
        <fileset dir="lib">
            <include name="**/*.jar"/>
        </fileset>
    </path>
    <taskdef name="db4o-enhance"
             classname="com.db4o.enhance.Db4oEnhancerAntTask"
             classpathref="project.classpath"/>    
    <!-- #end example -->
    
    
    <!-- #example: Define a target which runs the task #-->
    <target name="enhance">
        <db4o-enhance classtargetdir="${basedir}/bin"
                      jartargetdir="${basedir}/lib"
                      nq="true" ta="false"
                      collections="false">        	
            <classpath refid="project.classpath"/>
            <sources dir="${basedir}/bin">
                      <include name="**/*.class"/>
            </sources>
            <jars dir="${basedir}/lib">
                <include name="*.jar"/>
                <exclude name="db4o-*.jar"/>
                <exclude name="ant.jar"/>
                <exclude name="bloat-1.0.jar"/>
            </jars>
        </db4o-enhance>
    </target>
    
    <!-- #end example -->
    
</project>