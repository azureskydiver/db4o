package integration;

class Foo {
	private Object value;
	
	public Object value() {
		return value;
	}
}

class Main {
	static void main() {
		Foo foo = new Foo();
		int i = (((Integer) foo.value())).intValue();
		bar((((Integer) foo.value())).intValue());
		baz(new int[] { 42, (((Integer) foo.value())).intValue() });
		baz(new int[] { (((Integer) foo.value())).intValue(), 42 });
	}
	
	static void bar(int i) {
	}	
	
	static void baz(int[] i) {
	}
}