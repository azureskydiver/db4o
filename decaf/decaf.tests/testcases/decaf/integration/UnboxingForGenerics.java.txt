package integration;

class Foo<T> {
	private T value;
	
	public T value() {
		return value;
	}
}

class Main {
	static void main() {
		Foo<Integer> foo = new Foo<Integer>();
		int i = foo.value();
		bar(foo.value());
		baz(42, foo.value());
		baz(foo.value(), 42);
	}
	
	static void bar(int i) {
	}	
	
	static void baz(int... i) {
	}
} 